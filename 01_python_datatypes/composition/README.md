# Composition

Concept Overview

Demonstrates composition where a `Computer` is composed of `Processor`,
`Memory`, and `Storage` components.

Example

```py
from composition.composition_example import Computer
Computer().boot()
```

Expected Output

Component startup messages indicating composition flow.
