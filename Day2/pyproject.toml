[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "python-learning-framework"
version = "0.1.0"
description = "A professional Python learning repository with introspection, utilities, and CLI demos"
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
