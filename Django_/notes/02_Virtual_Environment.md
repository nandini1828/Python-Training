# Virtual Environment

A virtual environment isolates Python packages for each project.

Create one:
```bash
python -m venv .venv
```

Activate on macOS/Linux:
```bash
source .venv/bin/activate
```

Install packages:
```bash
pip install -r requirements.txt
```
