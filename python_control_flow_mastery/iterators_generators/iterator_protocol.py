"""Examples that explain the iterator protocol."""

from __future__ import annotations


def explain_iterator_protocol() -> str:
    """Return a short explanation of the iterator protocol."""
    return "Use iter() to get an iterator and next() to consume values."
