# Pass Statement in Python

## 📌 What is `pass`?

`pass` is a **null statement** in Python.

It does nothing — it is used as a **placeholder** where a statement is syntactically required.

---

## 🧠 Why do we need `pass`?

Python does not allow empty blocks.

```python
if True:
    # empty ❌ → SyntaxError