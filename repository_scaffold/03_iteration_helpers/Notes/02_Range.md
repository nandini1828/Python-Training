# 📌 range() in Python

---

## 🧠 What is `range()`?

`range()` is a built-in function used to generate a **sequence of numbers**.

It is most commonly used in loops to control how many times a loop runs.

---

## 🔹 Syntax

```python
range(stop)
range(start, stop)
range(start, stop, step)