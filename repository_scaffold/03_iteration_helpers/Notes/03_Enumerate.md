# 📌 enumerate() in Python

---

## 🧠 What is `enumerate()`?

`enumerate()` is a built-in function used to loop over an iterable **while keeping track of the index**.

Instead of manually managing counters, Python provides `enumerate()` to make code cleaner and safer.

---

## 🔹 Syntax

```python
enumerate(iterable, start=0)